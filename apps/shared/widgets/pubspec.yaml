name: widgets
description: Reusable Flutter UI widgets for The Isle applications
version: 1.0.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  models:
    git:
      url: https://github.com/PerfectFriend/simplex-node.git
      ref: v1.0.0-shared
      path: apps/shared/models

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true