name: widgets
description: Reusable Flutter UI widgets for The Isle applications
version: 1.0.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  models:
    path: ../models

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
