// Package acestep provides a client for the Acestep AI audio generation API,
// which runs on a separate laptop and converts text prompts into style-aware audio
// content: gospel-style adverts, tragic news broadcasts, royal decrees, and ambient
// island music. It includes a Client (Generate, GenerateAndCache, Healthy) and a
// Generator that manages AI-generated radio content and injects tracks into playlists.
package acestep
